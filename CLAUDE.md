# dev-digest

## First action: company workspace

Before inspecting or changing product code, read `.company/README.md`, then the
relevant files under `.company/memory/`. The registered project ID is
`dev-digest`. Treat this as a mandatory first step for every task.
<!-- company-workspace -->
