/** L13 — usage module literals. */

/** Free review runs per workspace per billing period. */
export const INCLUDED_RUNS = 200;

/** Cents charged per review run above the included allowance. */
export const OVERAGE_CENTS_PER_RUN = 5;
