# Changelog: dev-digest

## 2026-07-28 — TASK-001 (SMALL): company workspace initialized

- Committed the `.company/` workspace (memory, tasks, costs, `.gitignore` keeping
  `index/` ignored) generated by `scripts/company-map init`, plus additive
  company-pointer sections in `README.md`, `AGENTS.md`, and `CLAUDE.md`.
- Repo context: this clone is a fork (`mcmaxwell/dev-digest`); `origin` is the fork,
  `upstream` is the course repo `ai-agentic-engineering-neo/dev-digest` with push
  disabled. All delivery targets the fork's `main` only.
- QA smoke check approved: exact diff scope, index ignored, no secrets or absolute
  paths, additive README diff, `company-map validate` clean.
