{
  "name": "Add-repository screen renders",
  "description": "Loads /repos/new and confirms the add-repository form renders (heading + Repository URL field). Exercises the add-repository route and its extracted AddRepoView. Read-only: does not submit (no real clone/import, no backend mutation). The screen moved off /onboarding in L06 — that segment now belongs to the repo-scoped Onboarding Tour.",
  "steps": [
    { "cmd": ["open", "{BASE}/repos/new"], "label": "load the add-repository screen" },
    { "cmd": ["wait", "--url", "/repos/new"], "label": "add-repository route reached" },
    { "cmd": ["wait", "--text", "Add a repository"], "label": "the add-repository heading renders" },
    { "cmd": ["wait", "--text", "Repository URL"], "label": "the repository URL field renders" }
  ]
}
