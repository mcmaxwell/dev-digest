/* eval-case-modal — author or edit one eval case.

   Lives in src/components/ rather than inside the agents feature because two
   features open it: the Agent editor's Evals tab, and the PR findings list when
   a decided finding is turned into a case. */
export { EvalCaseModal } from "./EvalCaseModal";
