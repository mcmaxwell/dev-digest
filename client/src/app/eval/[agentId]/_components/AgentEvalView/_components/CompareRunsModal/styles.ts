import type { CSSProperties } from "react";

/** Co-located styles for CompareRunsModal. */
export const s = {
  body: { padding: 20, display: "flex", flexDirection: "column", gap: 20 } satisfies CSSProperties,
  deltas: { display: "flex", gap: 12 } satisfies CSSProperties,
  delta: {
    flex: 1,
    border: "1px solid var(--border)",
    borderRadius: 9,
    padding: 14,
    background: "var(--bg-surface)",
  } satisfies CSSProperties,
  deltaLabel: {
    fontSize: 10.5,
    fontWeight: 700,
    letterSpacing: "0.06em",
    color: "var(--text-muted)",
    marginBottom: 8,
  } satisfies CSSProperties,
  deltaRow: { display: "flex", alignItems: "baseline", gap: 7 } satisfies CSSProperties,
  from: { fontSize: 14, color: "var(--text-muted)", fontFamily: "var(--font-mono, monospace)" } satisfies CSSProperties,
  to: (color: string): CSSProperties => ({
    fontSize: 22,
    fontWeight: 700,
    color,
    fontFamily: "var(--font-mono, monospace)",
  }),
  chip: (color: string): CSSProperties => ({
    fontSize: 11.5,
    fontWeight: 700,
    color,
    marginLeft: 2,
  }),
  sectionLabel: {
    fontSize: 11.5,
    fontWeight: 700,
    letterSpacing: "0.06em",
    color: "var(--text-muted)",
    marginBottom: 8,
  } satisfies CSSProperties,
  note: { fontSize: 12, color: "var(--warn)", marginBottom: 10 } satisfies CSSProperties,
  table: { width: "100%", borderCollapse: "collapse", fontSize: 13 } satisfies CSSProperties,
  th: {
    textAlign: "left",
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: "0.05em",
    color: "var(--text-muted)",
    padding: "8px 10px",
  } satisfies CSSProperties,
  td: { padding: "9px 10px", borderTop: "1px solid var(--border)" } satisfies CSSProperties,
  mono: { fontFamily: "var(--font-mono, monospace)", fontSize: 12.5 } satisfies CSSProperties,
  prompt: {
    fontFamily: "var(--font-mono, monospace)",
    fontSize: 12,
    lineHeight: 1.6,
    border: "1px solid var(--border)",
    borderRadius: 9,
    padding: 12,
    background: "var(--bg-surface)",
    maxHeight: 260,
    overflowY: "auto",
    whiteSpace: "pre-wrap",
    margin: 0,
  } satisfies CSSProperties,
  added: { background: "color-mix(in srgb, var(--ok) 18%, transparent)", display: "block" } satisfies CSSProperties,
  removed: {
    background: "color-mix(in srgb, var(--crit) 18%, transparent)",
    display: "block",
    textDecoration: "line-through",
  } satisfies CSSProperties,
  footer: { display: "flex", justifyContent: "flex-end", gap: 8, padding: "14px 20px", borderTop: "1px solid var(--border)" } satisfies CSSProperties,
} as const;
